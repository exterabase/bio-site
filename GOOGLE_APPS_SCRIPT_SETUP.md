# 📊 Инструкция: Настройка счётчика просмотров через Google Apps Script

## Шаг 1: Создание Google Таблицы

1. Открой Google Drive (drive.google.com)
2. Создай новую Google Таблицу
3. Назови её "Lyzos Views Counter"
4. В ячейке A1 напиши: `views`
5. В ячейке A2 напиши: `0` (начальное значение)

## Шаг 2: Создание скрипта

1. В Google Таблице нажми: **Расширения** → **Apps Script**
2. Удали весь код в редакторе
3. Вставь следующий код:

```javascript
function doGet(e) {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('Лист1');
  
  // Получаем текущее значение просмотров
  var currentViews = sheet.getRange('A2').getValue();
  
  // Увеличиваем на 1
  var newViews = currentViews + 1;
  sheet.getRange('A2').setValue(newViews);
  
  // Возвращаем JSON с количеством просмотров
  var output = ContentService.createTextOutput(
    JSON.stringify({ views: newViews })
  );
  output.setMimeType(ContentService.MimeType.JSON);
  
  return output;
}
```

4. Нажми **Сохранить** (иконка дискеты)

## Шаг 3: Развёртывание

1. Нажми **Развернуть** → **Новое развёртывание**
2. Выбери тип: **Веб-приложение**
3. Настройки:
   - **Описание**: "View Counter API"
   - **Запускать от имени**: Мой аккаунт
   - **У кого есть доступ**: Все
4. Нажми **Развернуть**
5. **ВАЖНО**: Скопируй URL развёртывания (выглядит примерно так: `https://script.google.com/macros/s/AKfycby.../exec`)

## Шаг 4: Добавление URL в код

1. Открой файл `index.html`
2. Найди строку:
```javascript
const GOOGLE_SCRIPT_URL = "YOUR_GOOGLE_APPS_SCRIPT_URL_HERE";
```
3. Замени `YOUR_GOOGLE_APPS_SCRIPT_URL_HERE` на твой URL:
```javascript
const GOOGLE_SCRIPT_URL = "https://script.google.com/macros/s/AKfycby.../exec";
```

## Шаг 5: Проверка

1. Открой сайт в браузере
2. Счётчик должен увеличиться на 1
3. Проверь Google Таблицу — в A2 должно быть новое значение

## 🔧 Дополнительно: Защита от накрутки

Если хочешь, чтобы один пользователь считался только раз в день, код уже это делает через `localStorage`. Каждый уникальный посетитель будет добавлять +1 только раз в сутки.

## 📝 Примечания

- **Бесплатно**: Google Apps Script бесплатен для личного использования
- **Лимиты**: До 30 запросов в минуту (более чем достаточно)
- **Приватность**: Только ты видишь таблицу со статистикой
- **GitHub Pages**: Работает идеально с GitHub Pages

## ❓ Частые вопросы

**В: Счётчик показывает 1234**  
О: Это заглушка. Добавь URL Google Apps Script в код.

**В: Ошибка при выполнении скрипта**  
О: Убедись, что дал разрешение на выполнение скрипта (Google попросит при первом запуске).

**В: Хочу сбросить счётчик**  
О: Просто измени значение в ячейке A2 таблицы на 0.

## 🎉 Готово!

Теперь у тебя работающий счётчик просмотров без необходимости в сервере!
